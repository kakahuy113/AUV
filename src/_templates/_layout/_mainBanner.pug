// SLIDER INIT WHEN #js-page-verify have class "index-page"
section.MainSlider__Banners
	.swiper-container
		.swiper-wrapper
			.swiper-slide
				a.main_banner__link(href="#")
					// image-size: 1920x965 (index)
					// image-size: 665x665px (subpage)
					.main_banner__img.ov-h
						img.object-cover.fullsize(src=`${linkBanner}`, alt="BANNER")
		.swiper-pagination
	.pagesBanner__content
		.container.h-100
			.container-custom.size-1365px.h-100
				.pagesBanner__content__wrapper
					include _breadcrumb